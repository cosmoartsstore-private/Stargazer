# 仕様判断が必要な項目

作成日: 2026-06-20
更新日: 2026-07-28

## 調査範囲

- `desktop/src/features/` 配下の主要画面: イベント管理、応募管理、取込、抽選、マッチング、出欠、キャスト管理、NG 管理、投稿テンプレート
- `desktop/src/common/` の取込・ID 正規化処理
- `desktop/src/stores/` の localStorage 復元処理
- `desktop/src/db/repositories/` の応募者・抽選結果保存境界
- `desktop/src-tauri/src/lib.rs` のmigration・transaction・外部連携境界
- `desktop/src-tauri/windows/` のインストール・アンインストール処理
- `README.md` と現行テスト・カバレッジ設定

この一覧は未決定のプロダクト仕様を管理するものであり、すべてを確認済み不具合として扱うものではありません。安全性やデータ整合性に直結する実装不備は、仕様判断と分けて修正します。

## 解消済み・調査更新

- SG-SPEC-013: イベント管理画面は使用中イベントの削除ボタンを表示せず、使用していないイベントだけを削除できる既存仕様を維持する。
- SG-SPEC-018: `test.java` は存在しない。個人用の検証資料は `.claude/` に置く方針を `AGENT.md` へ記載した。
- SG-SPEC-019: ルートのUIデモ・監査HTMLは `.claude/design-references/` へ移し、内部設計資料として分類した。
- SG-SPEC-001: M000 はギフト抽選や学園イベントの抽選など、キャストを使わない単純抽選とする。出席キャスト未設定でも抽選画面へ進める。
- SG-SPEC-007: X ID が空、または正規化後に重複するデータ行が存在する場合は警告し、自動削除や暗黙の除外は行わない。新規取込と旧DBの既存行を同じ規則で検査し、利用者が該当行を手動削除するまで取り込み、抽選、マッチングへの遷移を許可しない。
- SG-SPEC-012: イベントは業務上の独立したイベントデータではなく、データを扱う区切りとして運用する。イベント名の一意性を維持し、既存の写真・説明メモ機能は維持する。旧版と同じく `db`、`logs`、`archive` もイベント名に使用できる。
- SG-SPEC-021: 一般的な Full HD モニターの全画面利用を基準とし、起動時は最大化する。通常ウィンドウへ戻した場合の既定サイズは 1920×1080 とし、最小サイズは固定しない。
- SG-SPEC-025: 希望キャストはイベント共有DBの安定IDで管理する。現在の名簿で解決できないIDや名前は警告だけを表示し、希望キャストとして判定できない可能性を伝える。抽選・マッチング・再実行・結果出力は制限しない。M000 は希望キャストを参照しないため警告も表示しない。IDの由来を確認できない旧形式データは、削除後の同名再登録を誤認しないよう名称からIDを推測せず、再取込時に確定する。
- SG-SPEC-003: M003を含む抽選・マッチング条件は取込セッションDBへ保存し、起動時も同じ方式として復元する。
- SG-SPEC-005: 要注意候補の閾値は既定値2とし、イベント共有DBの `settings` へイベント単位で保存する。自動候補は都度算出し、候補から追加したか手動登録したかの区分、NGキャスト数、理由・メモを固定登録後も保持する。候補集計と登録時の重複判定は保存表記の完全一致を使う。固定登録との応募者照合はX IDを必須とし、応募者名と登録名が両方ある場合は名前の一致も必要とする既存規則を維持する。
- SG-SPEC-002: アプリ内のDebug画面とサイドバー入口は完全に削除する。イベント未選択時はイベント管理とヘルプだけを利用可能にする。
- SG-SPEC-020: Discordボット、トークン保管、プロセス起動などの連携機能は中止済みであり、関連画面・Backend処理は現行ソースに残さない。キャスト連絡先として通常の外部リンクを開く機能は別機能として維持する。
- SG-SPEC-017: Frontend の coverage レポートは `desktop/src/coverage/` へ出力し、生成物は Git の管理対象に含めない。
- 保存済み抽選を開くと、当選者、保存時の抽選・マッチング方式、抽選人数を一括で復元する。
- 抽選条件・確定当選者・抽選結果は取込セッションDBを正とし、localStorageへ二重保存しない。テーマ、探索モード、最後に開いた位置だけを端末設定として保存する。

## 判断項目

| ID | 項目 | 現状 | 判断が必要な内容 | 影響範囲 |
|---|---|---|---|---|
| SG-SPEC-004 | NG 判定の基準 | 型上は `username` / `accountId` / `either` があるが、現行設定は X ID 相当の `accountId` 固定。既定挙動は NG を除外する。 | NG 判定は今後も `accountId` 固定でよいか。`warn` を既定にする運用があるか。 | マッチング結果、NG 管理、設定 UI、既存データ移行 |
| SG-SPEC-006 | X ID / VRChat 名の入力検証 | X ID は URL や `@id` を正規化するが、公式の長さ制限までは強制していない。VRChat URL も表示・保存中心で厳密検証はしていない。 | X handle 最大 15 文字、X 表示名最大 50 文字、VRChat Display Name 4〜15 文字を取込時に検証するか。表示だけ省略して保存値は受け入れるか。 | TSV 取込、応募者詳細、NG 管理、外部リンク |
| SG-SPEC-008 | 希望キャストのカンマ区切り上限 | カンマ区切りの希望キャストは最大 20 件に切り詰める。単一列扱いでは重複を削除し、分割列扱いでは最大 3 件を順位付きとして扱う。 | 最大 20 件でよいか。重複削除の方針を入力形式ごとに変えるか。希望順位なしの扱いを「同率希望」として明記するか。 | 取込、マッチングスコア、応募者詳細表示 |
| SG-SPEC-009 | 抽選バリデーションのエラー/警告境界 | 席数不足・キャスト不足などは error、余りや待機発生は warning。warning は確認して進める導線がある。 | warning を許容して実行できる条件を正式に定義するか。warning でも保存・マッチングへ進めるか。 | 抽選実行、マッチング品質、運営判断 |
| SG-SPEC-010 | 確定当選者の一覧表示 | 抽選設定カードでは1名を名前とX ID付きで表示し、残りは非表示人数を示す`+N`から選択モーダルへ誘導する。選択モーダルでも名前とX IDを表示する。 | 画面上で何名まで表示するか。`+N`クリックで選択モーダルを開く仕様でよいか。タッチ操作向けに別の全文表示UIが必要か。 | 抽選設定、確定当選者選択、アクセシビリティ |
| SG-SPEC-011 | 応募者詳細モーダルの表示方針 | 詳細モーダルは最大 760px 相当で、URL や自由記入欄は折り返し表示する。 | 長文回答は折り返しでよいか、固定高さスクロールやコピー操作を付けるか。 | 応募者確認、自由記入欄、VRC URL |
| SG-SPEC-014 | 出欠履歴の表示名 | 出欠履歴は安定した `cast_id` を保持し、表示時に現在のキャスト名を結合するため、改名後は過去履歴も新名称で表示する。 | 過去時点の名称を別途保存・表示する必要があるか。現名称へ追随する現行方針を正式仕様とするか。 | 出欠管理、キャスト管理、履歴表示 |
| SG-SPEC-015 | 保存済み当選者の保持方針 | 当選者、抽選・マッチング方式、抽選人数を保存し、読込時に一括復元する。表示名は`抽選結果（YYYY/MM/DD HH:mm・N名）`として自動生成し、保存上限・削除・リネームUIはない。 | 保存数の上限と削除UIを追加するか。現行の自動生成名を正式仕様とするか、リネームを許可するか。 | 抽選結果管理、DB サイズ、再抽選運用 |
| SG-SPEC-016 | 投稿テンプレートのプレースホルダー | 現行UIに表示するのは`{casts}`と`{event_name}`だけである。旧テンプレートに残る`{date}`、`{cast_count}`、`{tags}`は空文字へ置換する。 | 旧テンプレートの未対応値を今後も空文字化するか、利用者へ警告して削除を促すか。 | 投稿テンプレート、運営告知文 |
| SG-SPEC-022 | ヘルプ画面のサンプルUIの扱い | ヘルプ画面はサンプルContextを介して主要な実画面コンポーネントを固定サイズで描画する。実画面プレビューのアプリシェル全体へ`inert`を適用し、Contextの操作はno-opとして実操作を無効化する。ガイド本文と一部の補助表示は専用実装を持つ。 | 実画面流用と現在の操作無効化境界を保守契約として固定するか。 | ヘルプ、Context変更時の追随、保守コスト |
| SG-SPEC-023 | テーマトークンの公開契約 | 旧来のサービス名由来CSS変数を `--surface-*`、`--text-*`、`--accent-*`、`--button-*` へ置換した。現状はアプリ内部の実装契約として扱っている。 | テーマ追加者向けにトークン一覧を公開ドキュメント化するか。未使用・派生トークンをどこまで互換維持するか。 | テーマ追加、CSS保守、公開ドキュメント |
| SG-SPEC-024 | テーマカラー設定の保存スコープと拡張範囲 | デフォルトテーマはアクセント、背景色最大 5 色、方向、強度を localStorage に保存する。チェックテーマは色相だけを保存する。 | テーマ設定を端末全体のユーザー設定として固定するか。イベントごとに保存するか。プリセット保存、ランダム生成、インポート・エクスポートを追加するか。 | テーマUI、localStorage 移行、公開ドキュメント、利用者間の見た目共有 |
| SG-SPEC-026 | 旧形式データの移行先 | 旧global DB、旧イベント単一DB、イベント識別子のない旧localStorageを移行するUIはない。有効名のディレクトリはイベント一覧へ表示し、現行DBがなければopen時に作成する。起動時と通常アンインストールでは旧WebView profileを削除し、通常アンインストールは `Data/db` と `Data/logs` も削除する。 | 旧形式データを検出して保護するか、旧globalデータの移行先イベント、旧応募者データの移行先セッション、重複時の優先規則をどうするか。通常アンインストール時の保持範囲と、業務データをインストール先から分離するか。 | 既存利用者のデータ移行、バックアップ、移行UI、インストーラ |
| SG-SPEC-027 | 取込セッション履歴 | セッションがない場合だけ作成し、再取込は現在セッションを全置換する。過去版が作成した複数セッションは通常UIから選択・削除できない。 | 単一の現在データを正式仕様にして旧セッションを移行・archiveするか、履歴選択UIを復活させるか。 | DB容量、再取込、履歴閲覧、旧データ移行 |
| SG-SPEC-028 | 抽選条件変更の保存UI | 条件カウンターは画面へ即時反映し、DB書込みを呼出順に直列化する。最新の保存失敗時は最後の保存成功値へ戻す。 | 即時操作感を優先して現在の楽観更新を維持するか、操作中だけ入力をロックしてDB保存成功後に反映し、状態管理を単純化するか。 | 抽選条件UI、保存失敗時の表示、Context、Repository |
