# 仕様判断が必要な項目

作成日: 2026-06-20

## 調査範囲

- `desktop/src/features/` 配下の主要画面: イベント管理、応募管理、取込、抽選、マッチング、出欠、キャスト管理、NG 管理、投稿テンプレート
- `desktop/src/common/` の取込・ID 正規化処理
- `desktop/src/stores/` の localStorage 復元処理
- `desktop/src/db/repositories/` の応募者・抽選結果保存境界
- `README.md` と現行テスト・カバレッジ設定

## 判断項目

| ID | 項目 | 現状 | 判断が必要な内容 | 影響範囲 |
|---|---|---|---|---|
| SG-SPEC-001 | 抽選のみモードと出席キャスト必須チェック | 抽選タブへ進む前のチェックは出席キャスト 0 名をエラーにする。一方で M000 は抽選画面内では席数・ラウンド数・出席キャスト数を検証対象外にしている。 | M000 を使う前提なら、出席キャスト未設定でも抽選タブへ入れるべきか。抽選前チェック時点で M000 を選べない状態を許容するか。 | データ取込から抽選への導線、抽選前チェック、初回イベント運用 |
| SG-SPEC-002 | イベント未選択時に利用可能な画面 | イベント未選択時は応募管理・内部管理を非活性にし、イベント管理とヘルプだけを利用可能にしている。開発時の Debug は別扱い。 | Debug を開発時だけ常に許可するか。将来追加される画面をイベント必須にする既定方針を決めるか。 | サイドバー、ページ追加時の運用、テスト追加方針 |
| SG-SPEC-003 | M003 のセッション復元 | localStorage に保存された `matchingTypeCode` が M003 の場合、起動時は M001 に戻している。 | M003 を復元しない仕様で確定するか。復元しない理由を公開仕様へ記載するか。 | 起動時復元、抽選・マッチング条件、既存利用者の期待値 |
| SG-SPEC-004 | NG 判定の基準 | 型上は `username` / `accountId` / `either` があるが、現行設定は X ID 相当の `accountId` 固定。既定挙動は NG を除外する。 | NG 判定は今後も `accountId` 固定でよいか。`warn` を既定にする運用があるか。 | マッチング結果、NG 管理、設定 UI、既存データ移行 |
| SG-SPEC-005 | 要注意人物の自動候補閾値 | キャスト NG の重複数が既定 2 件以上で要注意候補になる。閾値は画面から変更できる。 | 既定値 2 を仕様として固定するか。イベントごとに保存するか、全体設定として保存するか。 | NG 管理、要注意人物候補、イベント間の設定共有 |
| SG-SPEC-006 | X ID / VRChat 名の入力検証 | X ID は URL や `@id` を正規化するが、公式の長さ制限までは強制していない。VRChat URL も表示・保存中心で厳密検証はしていない。 | X handle 最大 15 文字、X 表示名最大 50 文字、VRChat Display Name 4〜15 文字を取込時に検証するか。表示だけ省略して保存値は受け入れるか。 | TSV 取込、応募者詳細、NG 管理、外部リンク |
| SG-SPEC-007 | TSV 取込時の無効行の扱い | X ID が空の行があっても、有効な X ID が 1 件以上あれば取込可能。空 ID 行は結果的に保存対象から除外される運用になり得る。 | 空 X ID 行をエラーで止めるか、警告付きでスキップするか。重複 X ID を上書き・除外・エラーのどれにするか。 | 取込精度、応募者数、抽選対象、ユーザーへの説明 |
| SG-SPEC-008 | 希望キャストのカンマ区切り上限 | カンマ区切りの希望キャストは最大 20 件に切り詰める。単一列扱いでは重複を削除し、分割列扱いでは最大 3 件を順位付きとして扱う。 | 最大 20 件でよいか。重複削除の方針を入力形式ごとに変えるか。希望順位なしの扱いを「同率希望」として明記するか。 | 取込、マッチングスコア、応募者詳細表示 |
| SG-SPEC-009 | 抽選バリデーションのエラー/警告境界 | 席数不足・キャスト不足などは error、余りや待機発生は warning。warning は確認して進める導線がある。 | warning を許容して実行できる条件を正式に定義するか。warning でも保存・マッチングへ進めるか。 | 抽選実行、マッチング品質、運営判断 |
| SG-SPEC-010 | 確定当選者の一覧表示 | 抽選設定カードでは 1 名だけ表示し、残りは `+N` で選択モーダルへ誘導する。選択モーダル内は長い名前を省略し、全文は `title` に保持する。 | 画面上で何名まで表示するか。`+N` クリックで選択モーダルを開く仕様でよいか。タッチ操作向けに全文表示 UI が必要か。 | 抽選設定、確定当選者選択、アクセシビリティ |
| SG-SPEC-011 | 応募者詳細モーダルの表示方針 | 詳細モーダルは最大 760px 相当で、URL や自由記入欄は折り返し表示する。 | 長文回答は折り返しでよいか、固定高さスクロールやコピー操作を付けるか。 | 応募者確認、自由記入欄、VRC URL |
| SG-SPEC-012 | イベント名の制約 | イベント名は半角英数字、ハイフン、アンダースコアのみ。日本語名や表示名は別途持たない。 | DB 識別子と表示名を分けるか。日本語イベント名を許可するか。 | イベント管理、ファイルパス、既存イベント移行 |
| SG-SPEC-013 | イベント削除後の画面状態 | 現在イベントを削除すると `currentEventName` は null になる。メモリ上の応募者・キャスト・抽選結果を即時クリアするかは明確な公開仕様がない。 | 削除直後に関連状態を全クリアするか。次イベント選択まで保持表示しないだけでよいか。 | イベント管理、状態管理、誤操作防止 |
| SG-SPEC-014 | 出欠履歴のキー | 出欠履歴の集計はキャスト名をキーにしている。キャスト名変更時の過去履歴の扱いは明文化されていない。 | キャスト名変更時に履歴も追随するか、過去名として別人扱いにするか。安定 ID を導入するか。 | 出欠管理、キャスト管理、履歴集計 |
| SG-SPEC-015 | 保存済み抽選結果の保持方針 | 抽選結果スナップショットは保存できるが、保存上限・削除 UI・命名規則の仕様は明確ではない。 | 保存数の上限、削除・リネーム、保存名の形式を決めるか。 | 抽選結果管理、DB サイズ、再抽選運用 |
| SG-SPEC-016 | 投稿テンプレートのプレースホルダー | `{casts}` と `{event_name}` は置換されるが、`{date}`、`{cast_count}`、`{tags}` は空文字になる。 | 各プレースホルダーの正式な意味を定義するか。未対応プレースホルダーを UI から隠すか。 | 投稿テンプレート、運営告知文 |
| SG-SPEC-017 | coverage レポート出力先 | 外部ドライブ上で `desktop/coverage` の権限不整合が発生したため、coverage レポートは OS の一時領域へ出力している。 | CI や成果物確認で永続レポートが必要か。一時領域出力を正式な開発仕様にするか。 | テスト運用、README、CI 導入時の成果物 |
| SG-SPEC-018 | 未追跡ファイルの扱い | ルートに `test.java` が未追跡で存在する。今回の実装対象外のためコミットしていない。 | 削除、退避、`.gitignore` 追加のどれにするか。個人メモや検証ファイルの置き場所を `.claude/` 等へ統一するか。 | コミット運用、レビュー時の差分確認 |
| SG-SPEC-019 | ルート直下の HTML デモ/監査ファイル | ルートに UI デモ・監査系 HTML が複数存在する。公開成果物か、参考資料か、削除候補かが README から判断できない。 | ルートに残すファイル、docs へ移すファイル、agent-private へ移すファイルを分類するか。 | 公開ドキュメント、配布時の見え方、保守性 |
| SG-SPEC-020 | 外部サービス連絡先の正式対応範囲 | キャスト連絡先とヘルプ画面では Discord、X、VRChat を固定リンクとして扱う。内部のスタイル分類名は汎用化したが、UI 表示名はサービス名を維持している。 | Discord を正式対応サービスとして表示し続けるか。将来の入れ替えに備えて「チャット」「SNS」「VRChat」などの分類表示へ寄せるか。 | キャスト管理、ヘルプ、外部リンク導線、公開説明 |
| SG-SPEC-021 | 最低対応ウィンドウ幅 | 主要画面は狭幅時に1列化または横スクロールへ逃がす調整を行った。ただし Tauri デスクトップアプリとして正式な最小幅・最小高は明文化されていない。 | 最低対応幅を 1024px、768px、または別の値として定義するか。最小ウィンドウサイズを Tauri 設定で固定するか。 | 全画面レイアウト、スクリーンショット確認、配布設定 |
| SG-SPEC-022 | ヘルプ画面のサンプルUIの扱い | ヘルプ画面には静的なプレビューUIがあり、実コンポーネントや現在データとは独立している。命名は `guidePreview*` に統一した。 | 静的サンプルを保守対象として続けるか。実コンポーネントを流用するか。ヘルプ画面に表示するサンプルデータの範囲を定義するか。 | ヘルプ、デザイン変更時の追随、保守コスト |
| SG-SPEC-023 | テーマトークンの公開契約 | 旧来のサービス名由来CSS変数を `--surface-*`、`--text-*`、`--accent-*`、`--button-*` へ置換した。現状はアプリ内部の実装契約として扱っている。 | テーマ追加者向けにトークン一覧を公開ドキュメント化するか。未使用・派生トークンをどこまで互換維持するか。 | テーマ追加、CSS保守、公開ドキュメント |

## 優先確認が必要な項目

1. SG-SPEC-001: M000 をキャスト未設定でも実行可能にするか。
2. SG-SPEC-007: X ID 欠落・重複行を取込時に止めるか、警告で進めるか。
3. SG-SPEC-012: イベント名に日本語表示名を持たせるか。
4. SG-SPEC-014: 出欠履歴をキャスト名ではなく安定 ID で扱うか。
5. SG-SPEC-018: `test.java` の扱い。
6. SG-SPEC-021: 最低対応ウィンドウ幅と Tauri 側の最小サイズ。
